{
  "name": "aiway-vercel",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:html && npm run test:server",
    "test:html": "htmlhint index.html",
    "test:server": "node --check api/_utils.js && node --check api/ai.js && node --check api/models.js && node --check api/search.js && node --check api/health.js && node --check api/publish.js && node --check api/publish-check.js"
  },
  "devDependencies": {
    "htmlhint": "^1.6.3"
  }
}
